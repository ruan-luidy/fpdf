<UserControl
  x:Class="fpdf.Wpf.Views.Controls.PrintQueueControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ext="clr-namespace:fpdf.Wpf.Extensions"
  xmlns:hc="https://handyorg.github.io/handycontrol"
  xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:fpdf.Wpf.ViewModels"
  d:DataContext="{d:DesignInstance Type=vm:PrintQueueViewModel}"
  mc:Ignorable="d">

  <StackPanel Margin="10,0,0,0" Orientation="Horizontal">
    <!--  Impressora  -->
    <icon:PackIconPhosphorIcons
      Width="16"
      Height="16"
      Margin="0,0,8,0"
      VerticalAlignment="Center"
      Foreground="{DynamicResource PrimaryBrush}"
      Kind="Printer" />
    <TextBlock
      MaxWidth="300"
      VerticalAlignment="Center"
      Text="{Binding SelectedPrinter.DisplayName}"
      TextTrimming="CharacterEllipsis" />

    <!--  Contador da fila  -->
    <Button
      Margin="16,0"
      Click="OpenQueueDialog_Click"
      Style="{StaticResource ButtonIconCustom}"
      ToolTip="{ext:Loc PrintQueue_ViewQueue}">
      <StackPanel Orientation="Horizontal">
        <icon:PackIconPhosphorIcons
          Width="16"
          Height="16"
          Kind="Queue" />
        <TextBlock Margin="6,0,0,0" Text="{Binding PendingCountText}" />
      </StackPanel>
    </Button>

    <!--  Historico  -->
    <Button
      Margin="0,0,8,0"
      Click="OpenHistoryDialog_Click"
      Style="{StaticResource ButtonIconCustom}"
      ToolTip="{ext:Loc PrintHistory_Tooltip}">
      <icon:PackIconPhosphorIcons
        Width="16"
        Height="16"
        Kind="ClockCounterClockwise" />
    </Button>

    <!--  Botao Imprimir  -->
    <Button
      Command="{Binding PrintQueueCommand}"
      IsEnabled="{Binding IsPrinting, Converter={StaticResource Boolean2BooleanReConverter}}"
      Style="{StaticResource ButtonPrimary.Small}">
      <StackPanel Orientation="Horizontal">
        <icon:PackIconPhosphorIcons
          Width="16"
          Height="16"
          Kind="Printer" />
        <TextBlock Margin="8,0,0,0" Text="{ext:Loc PrintQueue_Print}" />
      </StackPanel>
    </Button>
  </StackPanel>
</UserControl>
